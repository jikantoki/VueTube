import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'xyz.enoki.vuetube',
  appName: 'VueTube',
  webDir: 'dist',
  androidScheme: 'https',
  hostname: 'vuetube.enoki.xyz',
}

export default config
